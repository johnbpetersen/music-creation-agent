{
  "name": "music-creation-agent",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.1.0"
  },
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "agent": "bun run src/agent.ts",
    "ui:build": "bun build src/ui/app.ts --outdir public/ui --target browser --minify",
    "typecheck": "bunx tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@coinbase/wallet-sdk": "^4.0.0",
    "@lucid-dreams/agent-kit": "latest",
    "hono": "^4.6.11",
    "dotenv": "^17.2.3",
    "zod": "^4.1.12",
    "viem": "^2.21.11"
  },
  "devDependencies": {
    "@types/node": "^24.7.2",
    "bun-types": "^1.3.0",
    "typescript": "^5.9.2"
  }
}
